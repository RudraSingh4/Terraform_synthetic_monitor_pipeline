name: applying
on: 
  workflow_dispatch:
    inputs:
      action:
        description: 'creating newrelic'
        required: true
        default: 'apply'
        options:
          - 'apply'
          - 'destroy'
env:
  BUCKET: workflow-bucket-synthetic-monitor
  TFSTATE_FILE: terraform.tfstate
  aws_region: us-east-1
#   pull_request:
#     branches:
#       - main
  # push:
  #   branches:
  #     - main
  
 
        
jobs:
  Test-monitor:
    runs-on : ubuntu-latest
    # strategy :
    #   matrix : { dir: ['Test-monitor', 'Test2-Monitor','Test3-Monitor', 'Test4-Monitor', 'Test5-Monitor', 'Test6-Monitor'  ] }
    steps:
      - name: "Setup - Checkout"
        uses: actions/checkout@v2.1.0
      - name: "Setup - Terraform CLI"
        uses: hashicorp/setup-terraform@v1
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
            aws-region: ${{ env.aws_region }}
      - name: Initialize Terraform
        run: terraform init
        working-directory: Test-monitor

      - name: Plan Terraform Changes
        run: terraform plan
        working-directory: Test-monitor

      - name: Apply Terraform Changes
        run: terraform apply -auto-approve
        working-directory: Test-monitor

      - name: Initialize Terraform
        run: terraform init
        working-directory: Test2-Monitor

      - name: Apply Terraform Changes
        run: terraform apply -auto-approve
        working-directory: Test2-Monitor

      # - name: Plan Terraform Changes
      #   run: |
      #     terraform init -reconfigure
      #     terraform destroy -auto-approve
        # working-directory: ${{ matrix.dir }}

      # - name: Store Terraform State in S3
      #   run: |
      #       for dir in Test-monitor Test2-Monitor; do
      #       cd $dir
      #       terraform state push terraform.tfstate
      #       aws s3 cp terraform.tfstate s3://workflow-bucket-synthetic-monitor/$dir/terraform.tfstate 
      #       done

      # if (github.event.inputs.action == "apply")
          #   then
          #     terraform apply -auto-approve
          # else
          #   terraform destroy -auto-approve
          # fi
        

       



#'Test2-Monitor', 